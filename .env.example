# Lovable Cloud Environment Variables
# These are automatically managed by Lovable Cloud
# All VITE_ prefixed variables are "publishable" keys (safe for frontend)

# Supabase Project ID
VITE_SUPABASE_PROJECT_ID="your-project-id"

# Supabase Publishable Key (anon key - safe for frontend)
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"

# Supabase URL
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# ⚠️ IMPORTANT SECURITY NOTES:
# - These variables are PUBLIC by design (VITE_ prefix exposes them to the browser)
# - Security is enforced via Row Level Security (RLS) policies in the database
# - Private API keys (OpenAI, Stripe, etc.) should NEVER be added here
# - Private secrets must be configured in Lovable Cloud's Edge Function Secrets
